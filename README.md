# fbx-viewer
 FBX format file viewer

## Build
```bash
mkdir build
cd build
cmake ..
cd ..
cmake --build build
cmake --install build
```

### TODO:
Maybe use 'https://github.com/bqqbarbhg/ufbx' instead of OpenFBX?